# IMPROVEMENTS.md — 第二轮改进需求与实施记录

> 记录时间：2026-09-11。来源：一期浏览器手工验收中发现的问题（对应 RUNBOOK §4.3 清单）。
> 本文档保存用户提出的 4 项改进要求，以及根因分析与实施情况。

---

## 需求 1：做题页评测结果不显示 +「提交不存在」误报

### 用户原话（要求）
- 普通用户做题界面，完成提交后**下方的评测结果区没有任何显示**，并且提交后**上方还无故提示「提交不存在」**。
- 改进要求：提交后下方的评测结果能**直接显示评测是否通过（AC/WA 等）**，并展示**输入用例、预期输出结果和实际输出结果**；同时把上方的错误提示修正。

### 根因分析
- `src/judge/judge_service.cpp` 的 `JudgeService::Submit()` 只把任务入队，`results_` 表中**没有该提交的记录**；
  worker 跑完后才由 `StoreResult()` 写入。而前端提交后立即轮询 `GET /api/submissions/{id}`，
  `JudgeService::Get()` 找不到记录 → 路由返回 404「提交不存在」→ 前端 `pollSubmission()` 抛错，
  `renderResult()` 永远不会执行 → 结果区空白。两个现象同源。
- `CaseResult` 结构体没有保存**用例输入**，前端想展示输入也无数据来源。

### 实施方案（已实现 ✅）
1. **后端** `judge_service.{h,cpp}`：`Submit()` 入队的同时立即创建一条
   `done=false`、状态为「评测中」的占位结果存入 `results_`，轮询接口立刻能查到，不再 404；
   worker 完成后覆盖为最终结果。
2. **后端** `CaseResult` 新增 `input` 字段（截断至展示上限），评测时写入；`submission_routes.cpp`
   的 JSON 序列化补充 `input` 与 `code` 字段。
3. **前端** `problem.html`：结果卡片按用例逐条展示 **输入 / 预期输出 / 实际输出** 三栏数据
   （`<details>` 折叠），首个 WA 用例仍保留顶部「预期 vs 实际」醒目对比；AC 时显示绿色通过状态。

---

## 需求 2：提交记录页增加「详情」

### 用户原话（要求）
- 提交记录界面，每一条提交记录加一个**类似「详情」的点击按钮**，用于查看该次提交的**用户代码**
  和需求 1 改进后的**评测结果**（含输入/预期/实际输出对比）。

### 根因分析
- `SubmissionResult` 未保存用户代码，`GET /api/submissions/{id}` 返回里没有 `code` 字段；
- `submissions.html` 每行只有状态摘要，无查看入口。

### 实施方案（已实现 ✅）
1. **后端** `SubmissionResult` 新增 `code` 字段（随结果存内存，上限 512KB）；详情接口返回。
2. **前端** `submissions.html`：每条记录右侧增加「详情」按钮，点击弹出模态框展示：
   提交状态 / 耗时内存 / **提交代码** / 逐用例评测明细（输入、预期输出、实际输出）/ 编译错误（CE 时）。

---

## 需求 3：管理后台增加「返回主界面」按钮

### 用户原话（要求）
- 管理员进入管理后台后只有登出按钮和修改题库功能，应增加一个**返回管理员主页界面的按钮**，
  用于回到主界面进行做题和查看提交结果。

### 实施方案（已实现 ✅）
- `admin.html` 顶栏（登出按钮旁）增加「返回主界面」按钮，链接到 `/problems.html`
  （普通导航条上管理员本就有「题目 / 提交记录 / 管理后台」入口）。

---

## 需求 4：UI 美化（黑绿色调 + 边框对齐 + 板块比例）

### 用户原话（要求）
- 做题界面各板块**参差不齐、大小比例不好**：**代码编辑器应做大一点，题目描述框过大**；
- 希望**整个界面的框边界对齐**；
- 不喜欢现在的白色亮色调，**全部换成黑绿色调**。

### 实施方案（已实现 ✅）
1. **配色** `static/css/style.css`：全站切换为暗色主题——近黑底色（带绿调）+ 绿色主强调色，
   覆盖登录/注册/首页/题库/做题/提交记录/管理后台所有页面与组件（卡片、按钮、徽章、
   表格、编辑器、代码块、语法高亮、滚动条）。
2. **对齐**：统一内容容器宽度（顶栏与内容区同宽），所有卡片同宽圆角边框，左右边界严格对齐。
3. **做题页比例**：左右分栏改为 **题目描述 5 : 代码编辑器 7**，描述区限制最大高度可滚动，
   编辑器高度加大（约 520px），结果卡片与上方两栏同宽对齐。

---

## 验证记录

- [x] `cmake --build build -j` 编译通过
- [x] 服务重启后健康检查 `{"status":"ok"}`
- [x] `bash scripts/test.sh` PASS=28 FAIL=0（28 项不回退）
- [x] 手工验证：提交后结果区直接显示 AC，无「提交不存在」误报；用例输入/预期/实际正常展示
- [x] 提交记录「详情」弹窗展示代码与逐用例结果
- [x] 管理后台「返回主界面」按钮跳转正常
