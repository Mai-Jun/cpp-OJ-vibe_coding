# admin.conf — 管理员账号配置（用户名/口令，不开放注册）
# 部署时请复制到 /etc/oj/admin.conf 并修改口令；服务端优先读取 /etc/oj/admin.conf，
# 若不存在则回退到仓库内 config/admin.conf。
#
# 警告：本文件含明文口令，属系统级配置，请确保其权限为 600（仅属主可读写）。

username=admin
password=CHANGE_ME_ADMIN_PASSWORD